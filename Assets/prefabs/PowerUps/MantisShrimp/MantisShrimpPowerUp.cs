using UnityEngine;

public class MantisShrimp : PowerUp
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       Strength = 20;
       PushPower = 20;
       Jump = 2; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
